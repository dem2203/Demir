# .streamlit/config.toml'ı güncelle
mkdir -p .streamlit

cat > .streamlit/config.toml << 'EOF'
[theme]
primaryColor = "#00FF00"
backgroundColor = "#0a0a0a"
secondaryBackgroundColor = "#1a1a1a"
textColor = "#ffffff"

[client]
showErrorDetails = false
EOF
